let age = 22;

const isAdult = age >= 18 ? "Yes, I am an adult." : "No, I am not an adult.";
console.log(isAdult);


let score = 85;

const grade = score >= 90 ? "A" :
              score >= 80 ? "B" :
              score >= 70 ? "C" :
              score >= 60 ? "D" : "F";

console.log("Grade: " + grade);