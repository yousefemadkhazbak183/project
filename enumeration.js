const dog = {
    name: 'Rex',
    age: 4,
    eyeColor: "Blue"
}

const key = Object.keys(dog);
console.log(key);

const va = Object.values(dog);
console.log(va);

const ent = Object.entries(dog);
console.log(ent);